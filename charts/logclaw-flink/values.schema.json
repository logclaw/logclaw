{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "LogClaw Flink Values",
  "type": "object",
  "properties": {
    "flink": {
      "type": "object",
      "properties": {
        "version": {"type": "string"},
        "etlJob": {
          "type": "object",
          "properties": {
            "enabled": {"type": "boolean"},
            "parallelism": {"type": "integer", "minimum": 1}
          }
        },
        "enrichmentJob": {
          "type": "object",
          "properties": {
            "enabled": {"type": "boolean"},
            "parallelism": {"type": "integer", "minimum": 1}
          }
        },
        "anomalyJob": {
          "type": "object",
          "properties": {
            "enabled": {"type": "boolean"},
            "parallelism": {"type": "integer", "minimum": 1}
          }
        }
      }
    }
  }
}
