apiVersion: v1
kind: Service
metadata:
  name: {{ include "logclaw-agent.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "logclaw-agent.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "logclaw-agent.selectorLabels" . | nindent 4 }}
